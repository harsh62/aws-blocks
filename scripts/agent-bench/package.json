{
	"name": "@aws-blocks/agent-bench",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Per-PR agent benchmark on Bedrock + Strands. Internal CI tooling only.",
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "tsc --noEmit"
	},
	"devDependencies": {
		"@strands-agents/sdk": "^1.6.0",
		"@types/node": "^20.0.0",
		"tsx": "^4.7.0",
		"typescript": "^5.3.0",
		"zod": "^4.1.12"
	}
}
